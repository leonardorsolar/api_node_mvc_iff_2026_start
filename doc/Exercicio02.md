# CRIANDO UM PROJETO ORIENTADO A OBJETOS

Exercício 3: Prompt :crie as rotas no arquivo server.js
GET `/api/users`,
GET `/api/users/:id`,
POST `/api/users`,
PUT `/api/users/:id`,
DELETE `/api/users/:id`,
explicando o que cada uma faz.

**userController.js**: 
Exercício 4: Prompt: implemente ***AS CLASSES** `getAllUsers`, `getUserByID`, `createUser`, `updateUser` e `deleteUser`, com explicação simples do fluxo no arquivo userController.js


**front end**: 
Exercício 5: Prompt: implemente no frontend no arquivo `index.html` e `script.js` as exibição das rotas
GET `/api/users`,
GET `/api/users/:id`,
POST `/api/users`,
PUT `/api/users/:id`,
DELETE `/api/users/:id`,


